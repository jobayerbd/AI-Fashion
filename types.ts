export interface Pose {
    name: string;
    prompt: string;
}

export interface AspectRatio {
    name: string;
    value: string;
}

export interface QualityOption {
    name: string;
    value: string;
    description: string;
}
